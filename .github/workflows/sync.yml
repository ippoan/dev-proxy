name: sync registry

# Aggregate .ippoan-dev.yaml across all ippoan org repos and open a PR if
# registry.json changed. No cron — runs only on explicit trigger:
#   - manual: `gh workflow run sync.yml`
#   - frontend merge → repository_dispatch type=registry-sync
# (Cron was removed because the registry changes slowly and a failing
#  scheduled run just adds noise.)

on:
  workflow_dispatch:
  repository_dispatch:
    types: [registry-sync]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Aggregate frontends → registry.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/sync.mjs --org ippoan --out registry.json

      - name: Open PR if changed
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git diff --quiet -- registry.json; then
            echo "no changes"
            exit 0
          fi

          BRANCH="sync/registry-$(date -u +%Y%m%d%H%M%S)"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add registry.json
          git commit -m "chore(registry): sync from ippoan org .ippoan-dev.yaml"
          git push -u origin "$BRANCH"

          gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "chore(registry): auto-sync from .ippoan-dev.yaml" \
            --body "Generated by .github/workflows/sync.yml (scripts/sync.mjs)." \
            --label automated || true
